apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: deploy-qs-dev
spec:
  template:
    spec:
      containers:
      - image: my-app-image
        ports:
           - containerPort: 8000
        resources:
          limits:
            cpu: 1000m
            memory: 1024Mi
